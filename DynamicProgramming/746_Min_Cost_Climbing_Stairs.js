/**
 * @param {number[]} cost
 * @return {number}
 */
var minCostClimbingStairs = function (cost) {
  let minCost = cost[0];
  let startIndex = 0;
};

let answer = minCostClimbingStairs([10, 15, 20]);
console.log(answer);
