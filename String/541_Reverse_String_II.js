/**
 * @param {string} s
 * @param {number} k
 * @return {string}
 */
var reverseStr = function (s, k) {
  let n = s.length;
  let res = "";
  let i = 0;
  // while (i < n) {
  //   if (k > n - i) {
  //   }
  //   i++;
  // }
};

let answer = reverseStr("abcdefg", 2);
console.log(answer);
